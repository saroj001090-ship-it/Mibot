import { ToolCard } from '@/components/tool-cards/ToolCard';import { categories, tools } from '@/lib/seo/tools';
export const metadata={title:'All Free PDF Tools',description:'Search every free forever, privacy-first DocuFlux PDF tool.'};
export default function Tools(){return <div className="container-page py-12"><h1 className="text-5xl font-black">All PDF tools</h1><p className="mt-4 max-w-2xl text-slate-600 dark:text-slate-300">Every tool is free forever with no premium badges, no locked exports, and no file-processing paywall.</p><input className="input my-8" placeholder="Search by tool, category, or keyword"/>{categories.map(c=><section key={c} className="mb-10"><h2 className="mb-4 text-2xl font-black">{c}</h2><div className="grid-tools">{tools.filter(t=>t.category===c || c==='AI PDF Tools'&&t.slug.includes('ocr') || c==='Batch Tools'&&['merge-pdf','compress-pdf','image-to-pdf','watermark-pdf'].includes(t.slug)).map(t=><ToolCard key={t.slug+c} tool={t}/>)}</div></section>)}</div>}
